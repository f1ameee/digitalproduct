;
(function () {

	let heroSlider = $('.single-slide');

	heroSlider.slick({
		dots: true,
		arrows: true,
		fade: true,
	});

	let imageSlider = $('.single-slide');

	imageSlider.slick({
		dots: true,
		arrows: true,
	});





})();